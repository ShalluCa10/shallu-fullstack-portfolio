extends layout

block content
  h1 Add Project

  form(method="POST")
    input(type="text", name="title", placeholder="Title")
    input(type="text", name="tech", placeholder="Tech Stack")
    input(type="text", name="github", placeholder="Github Link")
    textarea(name="description")
    button(type="submit") Save